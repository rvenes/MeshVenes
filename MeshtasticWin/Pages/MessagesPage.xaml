<Page
    x:Class="MeshtasticWin.Pages.MessagesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MeshtasticWin.Pages">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid ColumnDefinitions="Auto,12,*" VerticalAlignment="Center">
            <TextBlock Text="Messages" FontSize="30" FontWeight="SemiBold" />

            <ComboBox Grid.Column="2"
                      x:Name="ChatPicker"
                      ItemsSource="{x:Bind ChatTargets, Mode=OneWay}"
                      DisplayMemberPath="Display"
                      SelectionChanged="ChatPicker_SelectionChanged"
                      HorizontalAlignment="Left"
                      MinWidth="360" />
        </Grid>

        <!-- Messages -->
        <ListView Grid.Row="2"
                  x:Name="MessagesList"
                  ItemsSource="{x:Bind ViewMessages, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:MessageVm">
                    <StackPanel Spacing="4" Padding="10">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <TextBlock Text="{x:Bind Header}" FontWeight="SemiBold"/>
                            <TextBlock Text="{x:Bind When}" Opacity="0.7"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <!-- ✓ (heard) -->
                            <FontIcon Glyph="&#xE73E;"
              Foreground="Green"
              FontFamily="Segoe MDL2 Assets"
              Visibility="{x:Bind HeardVisible}" />

                            <!-- ✓✓ (delivered) -->
                            <FontIcon Glyph="&#xE73E;"
              Foreground="Green"
              FontFamily="Segoe MDL2 Assets"
              Visibility="{x:Bind DeliveredVisible}" />

                            <TextBlock Text="{x:Bind Text}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Composer -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="160"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="InputBox"
                     PlaceholderText="Skriv melding..."
                     AcceptsReturn="False"
                     KeyDown="InputBox_KeyDown"/>

            <Button Grid.Column="2"
                    Content="Send"
                    Click="Send_Click"/>
        </Grid>
    </Grid>
</Page>
